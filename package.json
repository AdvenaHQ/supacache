{
    "name": "supacache",
    "version": "1.1.6",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "cf-typegen": "wrangler types",
        "format": "biome check . --write --skip-errors",
        "bump-deps": "npx npm-check-updates --deep -u && pnpm up --latest --recursive"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.5.41",
        "@cloudflare/workers-types": "^4.20241230.0",
        "@biomejs/biome": "1.9.4",
        "@biomejs/cli-win32-x64": "^1.9.4",
        "@types/pako": "^2.0.3",
        "typescript": "^5.7.2",
        "wrangler": "^3.100.0"
    },
    "dependencies": {
        "compress-json": "^3.1.1",
        "pako": "^2.1.0"
    }
}
